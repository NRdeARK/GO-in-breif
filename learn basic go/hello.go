package main// every go file need to have this

import "fmt"// import fmt(format)  

func main() {// every thing start at here "main" function "{" is fixed to be there
	fmt.Println("Hello Gopher!!!")
}